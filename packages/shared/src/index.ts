export type Role = 'student' | 'teacher';
export type EventDTO = { id: string; teamId: string; title: string; startsAt: string; endsAt: string };
export type PostDTO = { id: string; userId: string; content: string; createdAt: string };
